import { FirstelementDirective } from './firstelement.directive';

describe('FirstelementDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstelementDirective();
    expect(directive).toBeTruthy();
  });
});
